
declare function local:name_validation($name) as xs:boolean
{
  return
    if (( $name/../tag[@k ='name'] and string-length($name/../tag[@k ='name']/@v/string()) >= 1))
};

where (condition) satisfies condition

$name/tag satisfies local:name_validation($name/tag[])
then
